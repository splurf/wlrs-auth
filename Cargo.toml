[package]
name = "wlrs-auth"
version = "0.1.0"
edition = "2021"

[dependencies]
argon2 = "0.5.3"
rand_core = { version = "0.6.4", features = ["getrandom"] }

[features]
build = []

[profile.release]
codegen-units = 1
lto = true
